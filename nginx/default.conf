server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # -------------------------
    # Static assets caching (must be above location /)
    # -------------------------
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf)$ {
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable" always;
        add_header X-Nginx-Cache "static-assets";
        try_files $uri =404;
    }

    # -------------------------
    # Prevent caching index.html
    # -------------------------
    location = /index.html {
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header X-Nginx-Cache "index-html";
    }

    # -------------------------
    # Test location block
    # -------------------------
    location /test {
        return 200 'Test block is working!';
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header X-Nginx-Cache "test";
    }

    # -------------------------
    # React Router support (must be last)
    # -------------------------
    location / {
        try_files $uri /index.html;
        add_header X-Nginx-Cache "fallback";
    }

    # -------------------------
    # Gzip compression
    # -------------------------
    gzip on;
    gzip_types
        text/plain
        text/css
        application/json
        application/javascript
        text/xml
        application/xml
        application/xml+rss
        image/svg+xml;
}

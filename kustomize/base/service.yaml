apiVersion: v1
kind: Service
metadata:
  name: gcp-cicd
  labels:
    app: gcp-cicd
  annotations:
    beta.cloud.google.com/backend-config: '{"default": "gcp-cicd-backendconfig"}'
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: gcp-cicd
